initiate (in file1)

Project1.main(new String[0]);

return value (in file2)

new newnew(tot).setVisible(true);

function (in file1)

    String total;
    newnew(String tot) {
        initComponents();
        this.total = tot;
        txttot.setText(String.valueOf(total));
        
    }
